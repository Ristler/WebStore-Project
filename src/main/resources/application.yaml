spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true

  application:
    name: webstore
  datasource:
    url: jdbc:mariadb://localhost:3306/projekti
    username: admin
    password: adminpass
