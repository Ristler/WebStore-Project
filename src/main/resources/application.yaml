spring:
  jpa:
    hibernate:
      ddl-auto: none # disabled to avoid ALTER on system-versioned tables (MariaDB error 4119)
    show-sql: true
    properties:
      hibernate.format_sql: true

  application:
    name: webstore
  datasource:
    url: jdbc:mariadb://localhost:3306/projekti
    username: admin
    password: xxxxx
